import{useState}from"react";import{Ionicons}from"@expo/vector-icons";import{router,useLocalSearchParams}from"expo-router";import{useMutation,useQuery,useQueryClient}from"@tanstack/react-query";import{Pressable,ScrollView,StyleSheet,Text,TextInput,View}from"react-native";import{customerApi,selectedBranchId}from"../services/api";import{Card,Chip,PageTitle,RemoteImage,Screen,State}from"../components/ui";import{colors}from"../constants/theme";import{useI18n}from"../services/i18n";
export default function Products(){const params=useLocalSearchParams<{q?:string}>(),{t}=useI18n();const[q,setQ]=useState(params.q??""),[category,setCategory]=useState(""),[sort,setSort]=useState("name"),[saved,setSaved]=useState(false);const client=useQueryClient();const branch=useQuery({queryKey:["selected-branch"],queryFn:selectedBranchId});const products=useQuery({queryKey:["products",q,category,sort,branch.data],queryFn:()=>customerApi.products({q,category,sort,branchId:branch.data??undefined}),enabled:branch.isFetched});const categories=useQuery({queryKey:["categories"],queryFn:customerApi.categories});const favs=useQuery({queryKey:["favourites"],queryFn:customerApi.favourites});const ids=new Set(favs.data?.map(x=>x.id));const toggle=useMutation({mutationFn:(id:string)=>ids.has(id)?customerApi.unfavourite(id):customerApi.favourite(id),onSuccess:()=>client.invalidateQueries({queryKey:["favourites"]})});const rows=saved?favs.data:products.data;return <Screen refreshing={products.isRefetching} onRefresh={()=>products.refetch()}><PageTitle title={t("productsTitle")} subtitle={t("productsSubtitle")} action={<Pressable style={s.scan} onPress={()=>router.push("/scanner")}><Ionicons name="barcode-outline" size={25} color={colors.blue}/></Pressable>}/><View style={s.search}><Ionicons name="search" size={20} color={colors.muted}/><TextInput style={{flex:1}} placeholder={t("productSearch")} value={q} onChangeText={setQ}/></View><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={s.chips}><Chip label={t("all")} active={!category&&!saved} onPress={()=>{setCategory("");setSaved(false)}}/>{categories.data?.map(x=><Chip key={x} label={x} active={category===x&&!saved} onPress={()=>{setCategory(x);setSaved(false)}}/>)}<Chip label={t("savedOnly")} active={saved} onPress={()=>setSaved(true)}/></ScrollView><Text style={s.filterTitle}>{t("sort")}</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={s.chips}>{[["name",t("sortName")],["price_asc",t("sortPriceAsc")],["price_desc",t("sortPriceDesc")],["discount",t("sortDiscount")]].map(([key,label])=><Chip key={key} label={label} active={sort===key} onPress={()=>setSort(key)}/>)}</ScrollView><State loading={products.isLoading||favs.isLoading} error={products.isError||favs.isError} retry={()=>products.refetch()} empty={!rows?.length?t("productsEmpty"):undefined}/>{rows?.map(p=><Card key={p.id} onPress={()=>router.push({pathname:"/product-detail" as never,params:{id:p.id}})}><View style={s.row}><RemoteImage url={p.image_url} height={92}/><View style={s.info}><Text style={s.name}>{p.name}</Text><Text style={s.meta}>{p.brand} · {p.category}</Text><Text style={s.availability}>{p.available===false?t("outOfStock"):t("inStock")}</Text><View style={s.prices}>{p.discount_price?<Text style={s.old}>{p.price.toFixed(2)} {t("currency")}</Text>:null}<Text style={s.price}>{(p.discount_price??p.price).toFixed(2)} {t("currency")}</Text></View></View><Pressable hitSlop={12} onPress={e=>{e.stopPropagation();toggle.mutate(p.id)}}><Ionicons name={ids.has(p.id)?"heart":"heart-outline"} size={27} color={colors.red}/></Pressable></View></Card>)}</Screen>}
const s=StyleSheet.create({scan:{width:46,height:46,borderRadius:15,backgroundColor:colors.softBlue,alignItems:"center",justifyContent:"center"},search:{height:54,backgroundColor:"white",borderWidth:1,borderColor:colors.border,borderRadius:15,paddingHorizontal:14,flexDirection:"row",alignItems:"center",gap:9},chips:{gap:8,paddingRight:10},filterTitle:{fontWeight:"800",color:colors.navy},row:{flexDirection:"row",gap:12,alignItems:"flex-start"},info:{flex:1},name:{fontWeight:"900",fontSize:16,color:colors.navy},meta:{color:colors.muted,fontSize:12,marginTop:3},availability:{color:colors.green,fontSize:12,fontWeight:"700",marginTop:5},prices:{flexDirection:"row",alignItems:"center",gap:7,marginTop:6},price:{color:colors.blue,fontWeight:"900",fontSize:18},old:{color:colors.muted,textDecorationLine:"line-through",fontSize:12}});
