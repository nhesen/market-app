import{Image,StyleSheet,Text}from"react-native";import{useLocalSearchParams}from"expo-router";import{useQuery}from"@tanstack/react-query";import{customerApi}from"../services/api";import{Card,PageTitle,Screen,State}from"../components/ui";import{colors}from"../constants/theme";
export default function NewsDetail(){const{id}=useLocalSearchParams<{id:string}>();const q=useQuery({queryKey:["news",id],queryFn:()=>customerApi.newsDetail(id!)});return <Screen><State loading={q.isLoading} error={q.isError}/>{q.data&&<><PageTitle title={q.data.title_az} subtitle={new Date(q.data.published_at).toLocaleDateString("az-AZ")}/>{q.data.image_url?.startsWith("http")?<Image source={{uri:q.data.image_url}} style={s.image}/>:<Card><Text style={s.visual}>MARKET XƏBƏRLƏRİ</Text></Card>}<Text style={s.body}>{q.data.summary_az}</Text><Text style={s.scope}>{q.data.branch_id?"Filial xəbəri":"Bütün market şəbəkəsi"}</Text></>}</Screen>}
const s=StyleSheet.create({image:{width:"100%",height:220,borderRadius:18},visual:{height:150,textAlign:"center",textAlignVertical:"center",fontSize:22,fontWeight:"900",color:colors.blue},body:{fontSize:16,lineHeight:25,color:colors.navy},scope:{color:colors.blue,fontWeight:"800"}});
