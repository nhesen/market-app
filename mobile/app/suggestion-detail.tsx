import{useLocalSearchParams}from"expo-router";import{useQuery}from"@tanstack/react-query";import{StyleSheet,Text,View}from"react-native";import{Card,PageTitle,Screen,State,Status}from"../components/ui";import{customerApi}from"../services/api";import{useI18n}from"../services/i18n";import{colors}from"../constants/theme";
export default function SuggestionDetail(){const{id}=useLocalSearchParams<{id:string}>(),{t,language}=useI18n();const q=useQuery({queryKey:["suggestion",id],queryFn:()=>customerApi.suggestion(id!),enabled:Boolean(id),refetchInterval:10000});const x=q.data;return <Screen><State loading={q.isLoading} error={q.isError} retry={()=>q.refetch()}/>{x?<><Status value={x.status}/><PageTitle title={x.title} subtitle={`${t("trackingNumber")}: ${x.tracking_number}`}/><Card><Text style={s.body}>{x.description}</Text></Card>{x.admin_note?<Card><Text style={s.noteTitle}>{t("managementNote")}</Text><Text>{x.admin_note}</Text></Card>:null}<Text style={s.section}>{t("timeline")}</Text><View style={s.event}><View style={s.dot}/><View><Status value="SUBMITTED"/><Text style={s.date}>{new Date(x.created_at).toLocaleString(language==="az"?"az-AZ":"en-GB")}</Text></View></View>{x.updated_at!==x.created_at?<View style={s.event}><View style={s.dot}/><View><Status value={x.status}/><Text style={s.date}>{new Date(x.updated_at).toLocaleString(language==="az"?"az-AZ":"en-GB")}</Text></View></View>:null}</>:null}</Screen>};const s=StyleSheet.create({body:{fontSize:15,lineHeight:23},noteTitle:{fontWeight:"900",color:colors.blue},section:{fontSize:19,fontWeight:"900",color:colors.navy},event:{flexDirection:"row",gap:12,paddingVertical:9},dot:{width:13,height:13,borderRadius:7,backgroundColor:colors.blue,marginTop:6},date:{fontSize:12,color:colors.muted,marginTop:5}});
