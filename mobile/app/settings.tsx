import{useState}from"react";import{StyleSheet,Switch,Text,View}from"react-native";import{Card,PageTitle,Screen}from"../components/ui";import{colors}from"../constants/theme";
const options=["Market xəbərləri","Endirimlər","Problem statusu","Təklif statusu","Bitən bonuslar","Filial elanları"];
export default function Settings(){const[state,setState]=useState<Record<string,boolean>>(Object.fromEntries(options.map(x=>[x,true])));return <Screen><PageTitle title="Bildiriş seçimləri" subtitle="Hansı məlumatları almaq istədiyinizi seçin"/><Card>{options.map(label=><View style={s.row} key={label}><Text style={s.label}>{label}</Text><Switch trackColor={{true:colors.blue}} value={state[label]} onValueChange={value=>setState(current=>({...current,[label]:value}))}/></View>)}</Card><Text style={s.note}>Push bildiriş provayderi demo rejimindədir. Status bildirişləri tətbiq daxilində saxlanılır.</Text></Screen>}
const s=StyleSheet.create({row:{minHeight:54,flexDirection:"row",alignItems:"center",justifyContent:"space-between",borderBottomWidth:1,borderColor:colors.border},label:{fontWeight:"700"},note:{color:colors.muted,lineHeight:20}});
